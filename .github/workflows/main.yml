name: CI

on: [push]

jobs:
  cwtools_job:
    runs-on: ubuntu-latest
    name: CWTools test
    steps:
    - uses: actions/checkout@v1
      with:
        fetch-depth: 1
    - name: CWTools test
      id: hello
      uses: Yard1/CWTools-action@master
